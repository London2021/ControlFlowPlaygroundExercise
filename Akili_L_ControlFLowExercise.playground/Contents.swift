import Cocoa

var timerInseconds = 180

for index in 1...180
    // the index creates the time elasped
{ print((index))
    
    if index < 180 { print ("Egg is not ready!")}
    
        if index == 180 { print ("Egg is ready!")}
}

 
